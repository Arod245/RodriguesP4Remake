@model IEnumerable<RodriguesP4.Models.Education>

@{
    ViewData["Title"] = "Index";
}

<h2>Education</h2>

<p>
    @if (User.IsInRole("admin"))
    {
        <a asp-action="Create">Create New</a>
    }

    <a asp-controller="Resume" asp-action="Index"></a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Degree)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.End)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.School)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Start)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Degree)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.End)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.School)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Start)
            </td>
            <td>
                @if (User.IsInRole("admin"))
                {
                    <a asp-action="Edit" asp-route-id="@item.EducationId">Edit</a> 
                }
                
                <a asp-action="Details" asp-route-id="@item.EducationId">Details</a> |
                @if (User.IsInRole("admin"))
                {
                    <a asp-action="Delete" asp-route-id="@item.EducationId">Delete</a> 
                }
                

            </td>
        </tr>
}
    </tbody>
</table>
